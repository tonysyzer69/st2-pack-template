---
name: d42
description: add ip address to powerdns
runner_type: local-shell-cmd
entry_point: ""
enabled: true
parameters:
  sudo:
    immutable: true
  kwarg_op:
    immutable: true
  cmd:
    description: "Command to run"
    type: string
    immutable: true
    default: "/usr/bin/d42 {{ servername }}
  servername:
    description: "server name"
    type: string
    required: true
