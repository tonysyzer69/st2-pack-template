---
name: chatops.ansible_terraform_fos_gridvcenter01
action_ref: st2_chatops_aliases.terraform_fos_gridvcenter01
description: run terraform
formats:
  - display: "terraform_fos_gridvcenter01 <package> <hosts>"
    representation:
      - "terraform_fos_gridvcenter01 {{ package }} {{ hosts }}"
result:
  format: |
    terraform_fos_gridvcenter01 `{{ execution.parameters.package }}` `{{ execution.parameters.hosts }}`
