---
name: chatops.terraform-plan
action_ref: st2_chatops_aliases.terraform-plan
description: terraform-plan
formats:
  - display: "terraform-plan"
    representation:
      - "terraform-plan"
ack:
  enabled: false
result:
  format: |
    {% if execution.status == 'succeeded' %}Here is your output: ```{{ execution.result.stdout }}``` {~}{% else %}
    Sorry, no command this time {~}
    Exit Code: `{{ execution.result.return_code }}`
    Stderr: ```{{ execution.result.stderr }}```
    Hint: Make sure `terraform-plan` utility is installed.
    {% endif %}
