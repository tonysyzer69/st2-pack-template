---
name: chatops.d42
action_ref: st2_chatops_aliases.d42
description: add record to powerdns
formats:
  - display: "d42 <servername>"
    representation:
      - "d42 {{ servername }}"
ack:
  enabled: false
result:
  format: |
    {% if execution.status == 'succeeded' %}Here is your output: ```{{ execution.result.stdout }}``` {~}{% else %}
    Sorry, no ouput this time {~}
    Exit Code: `{{ execution.result.return_code }}`
    Stderr: ```{{ execution.result.stderr }}```
    Hint: Make sure `d42` utility is installed.
    {% endif %}
